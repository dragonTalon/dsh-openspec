# dsh-openspec bundle layer: register the OpenSpec skill plugin.
# Installed via `dsh plugin --profile <name> add @dragonTalon/dsh-openspec`.
- insert:
    - id: dsh-openspec
      name: '@dragonTalon/dsh-openspec'
